{
  "name": "@tao/daemon",
  "version": "1.0.0",
  "private": true,
  "description": "Tao persistence daemon skeleton",
  "scripts": {
    "build": "zig build",
    "check": "zig build run -- --check",
    "fmt": "node ../../scripts/zig-tools.mjs fmt apps/daemon",
    "fmt:check": "node ../../scripts/zig-tools.mjs fmt:check apps/daemon",
    "lint": "node ../../scripts/zig-tools.mjs lint apps/daemon",
    "lsp": "zls --version",
    "test": "zig build test"
  }
}
