{
  "name": "@tao/daemon",
  "version": "1.0.0",
  "private": true,
  "description": "Tao persistence daemon",
  "type": "module",
  "scripts": {
    "build": "node --experimental-strip-types ../../scripts/taod.ts build",
    "check": "node --experimental-strip-types ../../scripts/taod.ts check",
    "fmt": "node --experimental-strip-types ../../scripts/zig-tools.ts fmt apps/daemon",
    "fmt:check": "node --experimental-strip-types ../../scripts/zig-tools.ts fmt:check apps/daemon",
    "lint": "node --experimental-strip-types ../../scripts/zig-tools.ts lint apps/daemon",
    "lsp": "zls --version",
    "test": "node --experimental-strip-types ../../scripts/taod.ts test"
  }
}
