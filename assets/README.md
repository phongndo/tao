# Assets

Shared repository assets live here.

Nightly-channel artwork lives under `assets/nightly` and is copied into the desktop renderer build.
App-specific runtime assets remain under the owning app, such as `apps/desktop/public`.
