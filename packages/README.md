# Packages

Shared workspace packages live here.

Current packages:

- `@tao/shared`: app commands, workspace schemas, IPC payload types, and typed workspace errors shared across Electron main, preload, and renderer code.
