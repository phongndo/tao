# Packages

Shared workspace packages live here.

Current packages:

- `@tau/shared`: app commands, workspace schemas, IPC payload types, and typed workspace errors shared across Electron main, preload, and renderer code.
